name: Test

on: push

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Hello World
        run: |
          echo "${{ vars.WEBSITE }}"
          echo "${{ secrets.API_KEY }}"